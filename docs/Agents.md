# Agents (mockups branch)

This file mirrors the **UX/UI designer** and automation notes from the repository root **`Agents.md`** on the **`mockups`** branch so in-site links work on GitHub Pages.

**Full source (always current):** [Agents.md on GitHub — `mockups` branch](https://github.com/shahzebqazi/PowerAmpache2PluginTemplate/blob/mockups/Agents.md)

**Quick pointers**

- Design and research live on **`mockups`** (`docs/`, `mockups/`); **no** Kotlin or Gradle **`app/`** at this branch’s root.
- **Implementation** is on **`main`** (or feature branches merged to **`main`**).
- Figma and frame labels: [Prototype and engineering](agents/prototype-and-engineering.md), [Mockups folder](mockups.md).

---

*If this stub drifts from root `Agents.md`, update the GitHub link above or sync the content.*
