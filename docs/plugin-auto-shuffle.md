# plugin/auto-shuffle

ExoPlayer `COMMAND_SET_SHUFFLE_MODE` + session `setMediaButtonPreferences` (Media3 demo pattern) for Android Auto.

**DHU:** Play 3+ tracks → Now Playing → **⋮ overflow** → Shuffle. `adb shell dumpsys media_session | grep -i shuffle`
