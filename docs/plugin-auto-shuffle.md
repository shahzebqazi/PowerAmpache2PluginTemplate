# plugin/auto-shuffle

ExoPlayer `COMMAND_SET_SHUFFLE_MODE` + session `CommandButton` custom layout for Android Auto.

**DHU:** Play 3+ tracks → Now Playing → Shuffle in transport/overflow. `adb shell dumpsys media_session | grep -i shuffle`
